--- Thermal Battery Runtime Logic
--- Handles any runtime behavior for thermal batteries (currently minimal).

local tenebris = require("lib.tenebris")

local thermal_battery = {}

-- Currently no special runtime logic needed.
-- The composite system handles creation/destruction.
-- The heat-interface handles heat storage automatically.

return thermal_battery

